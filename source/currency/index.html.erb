---
typeof: CollectionPage
title: HTML Currency Symbols, Entities and Codes — HTML Arrows
description: The most beautiful HTML currency symbol codes and ASCII characters you will ever see, plus all those other HTML, ASCII and HEX codes you can never remember.
image: 
changefreq: "daily"
priority: 1.0
---

<%# Meta Tags %>
<% content_for :meta do %>
  <%= partial("meta/meta_index") %>
<% end %>

<%# Colors %>
<% content_for :stylesheets do %>
	article {background-color: rgb(65, 80, 80);}
<% end %>

<%# Page Content %>
<section id="index" class="row grid currency">

	<%= partial("partials/table") %>

	<% data.currency.entities.each do |entity| %>
		<%= partial("partials/entity_index", :locals => { :entity => entity }) %>
	<% end %>

</section>

<%# Page Footer %>
<% content_for :breadcrumbs do %>
  <span typeof="v:Breadcrumb">
  	<%= link_to "Home", "/", rel: "v:url", property: "v:title" %> &#8250;
    <span rel="v:child" typeof="v:Breadcrumb">
      <%= link_to "Currency", "/currency/", rel: "v:url", property: "v:title" %>
    </span>
  </span>
<% end %>