---
typeof: CollectionPage
title: HTML Math Symbols, Entities and Codes — HTML Arrows
description: The most beautiful HTML math symbols you will ever see, including plus, minus, multiply, divide and equal sign HTML codes, plus a panoply of others.
image: 
changefreq: "daily"
priority: 1.0
---

<%# Meta Tags %>
<% content_for :meta do %>
  <%= partial "meta/meta_index" %>
<% end %>

<%# Colors %>
<% content_for :stylesheets do %>
	article {background-color: rgb(70, 55, 65);}
<% end %>

<%# Page Content %>
<section id="index" class="row grid math">

	<%= partial("partials/table") %>

	<% data.math.entities.each do |entity| %>
		<%= partial("partials/entity_index", :locals => { :entity => entity }) %>
	<% end %>

</section>

<%# Page Footer %>
<% content_for :breadcrumbs do %>
  <span typeof="v:Breadcrumb">
  	<%= link_to "Home", "/", rel: "v:url", property: "v:title" %> &#8250;
    <span rel="v:child" typeof="v:Breadcrumb">
      <%= link_to "Math", "/math/", rel: "v:url", property: "v:title" %>
    </span>
  </span>
<% end %>