---
typeof: CollectionPage
title: HTML Arrow Symbols, Entities and Codes — HTML Arrows
description: The most beautiful HTML arrow symbols you will ever see, including right, left, up, down and ordinal arrow HTML codes, plus a panoply of others.
image: 
changefreq: "daily"
priority: 1.0
---

<%# Meta Tags %>
<% content_for :meta do %>
  <%= partial("meta/meta_index") %>
<% end %>

<%# Colors %>
<% content_for :stylesheets do %>
	article {background-color: rgb(75, 95, 95);}
<% end %>

<%# Page Content %>
<section id="index" class="row grid arrows">

	<%= partial("partials/table") %>

	<% data.arrows.entities.each do |entity| %>
		<%= partial("partials/entity_index", :locals => { :entity => entity }) %>
	<% end %>
	
</section>

<%# Page Footer %>
<% content_for :breadcrumbs do %>
  <span typeof="v:Breadcrumb">
  	<%= link_to "Home", "/", rel: "v:url", property: "v:title" %> &#8250;
    <span rel="v:child" typeof="v:Breadcrumb">
      <%= link_to "Arrows", "/arrows/", rel: "v:url", property: "v:title" %>
    </span>
  </span>
<% end %>